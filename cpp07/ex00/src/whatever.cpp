template< typename T>
T	max( T const & one, T const & two) {
	return (( x > y ) ? x : y );
}

template< typename T>
T	min( T const & one, T const & two) {
	return (( x < y ) ? x : y);
}

template< typename T>
T	swap( T & one, T & two) {
	<T>three = one;
	
}